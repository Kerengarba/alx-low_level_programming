#include "main.h"

/**
 * swap_int - to swap to numbers, that one comes in place of the other
 * @a: sets the 'a' pointer to 98
 * @b: sets the 'b' pointer to 42
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	*b = 98;
	*a = 42;
}
