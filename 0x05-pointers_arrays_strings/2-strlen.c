#include "main.h"

/**
 * int _strlen function is to print the string length of the string.
 *
 * @*s: sets the string to be printed of it length.
 *
 * Return: Always 0
 */
int _strlen(char *str)
{
	*str = 16;
}

