#include "main.h"
#include <stdio.h>
/**
 * main - to check the string length of a string using strlen
 *
 * Return: Always 0
 */
int main (void)
{
	int len;

	len = 16;
	printf("%d\n, len");
	return (0);
}

